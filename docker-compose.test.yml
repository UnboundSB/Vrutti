version: '3.8'

services:
  test-suite:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - .:/app
    command: >
      bash -c "mkdir -p build-linux && cd build-linux && cmake .. && cmake --build . && cd .. && node tests/run_node_tests.js"
